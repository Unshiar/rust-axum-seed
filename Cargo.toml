[package]
name = "untitled"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.9"
migration = { path = "migration" }
sea-orm = { version = "2.0.0-rc.41", features = ["sqlx-postgres", "runtime-tokio"] }
serde = { version = "1.0.228", features = ["derive"] }
tokio = { version = "1.52.3", features = ["full"] }
serde_repr = "0.1.20"

[workspace]
members = [
    ".",          # Ваше текущее основное приложение (src)
    "migration"   # Созданный пакет миграций
]
