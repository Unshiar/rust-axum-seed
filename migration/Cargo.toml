[package]
edition      = "2021"
name         = "migration"
publish      = false
rust-version = "1.96.0"
version      = "0.2.0"

[lib]
name = "migration"
path = "src/lib.rs"

[dependencies]
tokio = { workspace = true }
sea-orm-migration = { workspace = true }
entities = { path = "../entities" }
